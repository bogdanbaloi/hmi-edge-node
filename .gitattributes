# Language statistics (GitHub Linguist).
# The startup assembly and the linker scripts are vendor-generated by
# STM32CubeIDE, not hand-written. Mark them as vendored so Linguist keeps
# them in the repo but drops them from the language breakdown, leaving the
# stats to reflect the real hand-written work: C.
Startup/startup_stm32l476rgtx.s linguist-vendored
*.ld                            linguist-vendored
